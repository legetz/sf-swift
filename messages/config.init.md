# description
Create a .swiftrc configuration file in the current directory using SF Swift defaults. If a .swiftrc already exists, it is backed up before being overwritten.

# output.backup.created
📁 Backup created: %s

# output.created
🪄 Successfully created config file %s using built-in defaults. Adjust config and commit it to GIT 🤓

# examples
- Create a .swiftrc file in the current directory:
<%= config.bin %> <%= command.id %>
